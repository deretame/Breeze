name: Test macOS Build & DMG

on:
  workflow_dispatch:

jobs:
  test-build-macos:
    runs-on: macos-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 安装系统依赖 (C 底层库)
        env:
          GIT_CRYPT_KEY: ${{ secrets.GIT_CRYPT_KEY }}
        run: |
          # 安装 Rust 编译和底层压缩需要的库
          brew install git-crypt pkg-config xz bzip2

          # 解密敏感文件
          echo "$GIT_CRYPT_KEY" | base64 --decode > ./git-crypt-key || echo "$GIT_CRYPT_KEY" | base64 -D > ./git-crypt-key
          git-crypt unlock ./git-crypt-key && rm ./git-crypt-key

      - name: 设置 Dart SDK
        uses: dart-lang/setup-dart@v1

      - name: 设置 Flutter (FVM)
        run: |
          dart pub global activate fvm
          echo "$HOME/.pub-cache/bin" >> $GITHUB_PATH
          export PATH="$PATH:$HOME/.pub-cache/bin"
          fvm install && fvm use --force
          echo "$GITHUB_WORKSPACE/.fvm/flutter_sdk/bin" >> $GITHUB_PATH

      - name: 安装 Rust 工具链 (确保支持 arm64)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin, x86_64-apple-darwin

      - name: 安装打包工具
        run: brew install create-dmg

      - name: 拉取依赖
        run: fvm flutter pub get

      - name: 🛠️ 核心修复：强制链接 C 库与版本同步
        run: |
          # 1. 修改 Podfile 处理 CocoaPods 的版本拦截
          if [ -f "macos/Podfile" ]; then
              perl -pi -e "s/^#?\s*platform\s+:osx.*/platform :osx, '11.0'/g" macos/Podfile
              perl -pi -e 's/(post_install do \|installer\|)/$1\n  installer.pods_project.targets.each do |target|\n    target.build_configurations.each do |config|\n      config.build_settings["MACOSX_DEPLOYMENT_TARGET"] = "11.0"\n    end\n  end/g' macos/Podfile
          fi

          # 2. 【核心】使用 Ruby 直接改写 Xcode 工程数据库，解决 arm64 链接报错
          gem install xcodeproj
          ruby -e "
          require 'xcodeproj'
          project = Xcodeproj::Project.open('macos/Runner.xcodeproj')
          

          # 定义 Homebrew 在 ARM 架构下的库路径
          lib_paths = [
            '$(inherited)',
            '/opt/homebrew/lib',
            '/opt/homebrew/opt/xz/lib',
            '/opt/homebrew/opt/bzip2/lib'
          ]
          
          project.targets.each do |target|
            target.build_configurations.each do |config|
              # 强制统一最低部署版本为 11.0
              config.build_settings['MACOSX_DEPLOYMENT_TARGET'] = '11.0'
              
              if target.name == 'Runner'
                # A. 注入库搜索路径 (解决找不到 .a 文件的根本问题)
                current_libs = config.build_settings['LIBRARY_SEARCH_PATHS'] || []
                current_libs = [current_libs] if current_libs.is_a?(String)
                config.build_settings['LIBRARY_SEARCH_PATHS'] = (current_libs + lib_paths).uniq
                
                # B. 注入链接参数 (显式告知链接 lzma 和 bz2)
                current_flags = config.build_settings['OTHER_LDFLAGS'] || []
                current_flags = [current_flags] if current_flags.is_a?(String)
                config.build_settings['OTHER_LDFLAGS'] = (current_flags + ['-llzma', '-lbz2']).uniq
              end
            end
          end
          
          project.save
          puts '✅ Xcode 数据库已强行注入 arm64 链接补丁！'
          "

      - name: 编译 Flutter macOS
        run: fvm flutter build macos --release

      - name: 制作 DMG
        run: |
          APP_PATH=$(find build/macos/Build/Products/Release -name "*.app" -maxdepth 1 | head -n 1)
          APP_NAME=$(basename "$APP_PATH")
          rm -f "Breeze-macOS-Test.dmg"
          create-dmg \
            --volname "Breeze Installer" \
            --window-pos 200 120 \
            --window-size 600 400 \
            --icon-size 100 \
            --icon "$APP_NAME" 150 190 \
            --hide-extension "$APP_NAME" \
            --app-drop-link 450 190 \
            "Breeze-macOS-Test.dmg" \
            "$APP_PATH"

      - name: 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: macOS-DMG-Test
          path: Breeze-macOS-Test.dmg
          retention-days: 3
